{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "exporter",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/speedtest_exporter/main.go",
            "cwd": "${workspaceFolder}",
        },
    ],
}